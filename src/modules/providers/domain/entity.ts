export enum Provider {
  NRW = "NRW",
  TLS = "TLS"
}

export type Label = string

export enum Status {
  RECEIVED = "RECEIVED",
  PROCESSING = "PROCESSING",
  TRANSIT = "TRANSIT",
  DELIVERED = "DELIVERED",
}
